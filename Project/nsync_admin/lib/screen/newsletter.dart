import 'package:flutter/material.dart';

class NewsLetterScreen extends StatelessWidget {
  const NewsLetterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("NewsLetter Page"),
    );
  }
}
